import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  // here is the root styles
  root: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
    padding: 10,
    paddingTop: 40,
  },
});

export default styles;
